---
name: verify
description: Run formatting checks to validate code before committing
---

Run the following verification steps and report results:

1. **Format check**: Run `pnpm run format:check` to verify all files are properly formatted.

If formatting issues are found, ask the user whether to auto-fix with `pnpm run format`.
